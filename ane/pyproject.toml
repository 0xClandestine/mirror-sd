[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "mirror-sd-ane"
version = "0.1.0"
description = "Python bindings for running DFlash draft model on Apple Neural Engine"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
